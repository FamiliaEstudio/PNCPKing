<Window x:Class="PNCPKing.App.Views.DataFolderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PNCP King — pasta de dados"
        Width="660" Height="245" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="22" FontWeight="SemiBold" Text="Onde guardar o banco do PNCP King?" />
        <TextBlock Grid.Row="1" Margin="0,8,0,10" TextWrapping="Wrap"
                   Text="A carga nacional pode ocupar vários gigabytes. Escolha uma unidade com espaço suficiente; o tamanho será confirmado antes do download." />
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="FolderTextBox" VerticalContentAlignment="Center" />
            <Button Grid.Column="1" Content="Procurar…" Click="Browse_Click" />
        </Grid>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancelar" IsCancel="True" />
            <Button Content="Usar esta pasta" IsDefault="True" Click="Accept_Click" />
        </StackPanel>
    </Grid>
</Window>
